Starten der DB: ausführen von starten.sh
Kompilieren des Schemas: kompilieren.sh
Kompilieren der Stored Procedures und des Clients: Kompilieren_java.sh

Beenden der DB: voltadmin shutdown

Browser DB-Status: localhost:8080




country-codes: ISO 3166-1 Alpha 2



SQL:

Die Karte mit der höchsten Transfersumme:

select top 1 card_num, sum(amount) as money from transfer group by card_num order by money desc;

